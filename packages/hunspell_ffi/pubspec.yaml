name: hunspell_ffi
description: "Hunspell morphology bindings for Ciyue."
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  code_assets: ^2.0.0
  ffi: ^2.1.4
  hooks: ^2.2.0
  native_toolchain_c: ^0.19.4

dev_dependencies:
  ffigen: ^20.1.1
  lints: ^6.0.0
  test: ^1.28.0
